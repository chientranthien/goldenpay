apiVersion: v1
kind: Service

metadata:
  name: event_handler
spec:
  ports:
    - port: 5000
  selector:
    app: goldenpay
    name: event_handler
  clusterIP: None

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: goldenpay-be-deployment-event_handler
  labels:
    app: goldenpay
spec:
  replicas: 1
  selector:
    matchLabels:
      app: goldenpay
      tier: be
  template:
    metadata:
      labels:
        app: goldenpay
        name: event_handler
        tier: be
    spec:
      containers:
        - name: goldenpay-event_handler
          image: chientt1993/goldenpay-be-event_handler
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 5000
